# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local

# Vercel
.vercel

# Rendered PDF page images (~431 MB) - served from Supabase Storage in production.
# Keep locally for dev; do not commit. See tools/upload_to_supabase.ts.
public/pdfs-rendered/

# Source PDFs (~232 MB; Duale Reihe alone is 114 MB and exceeds GitHub's 100 MB
# single-file limit). Used only by tools/upload_to_supabase.ts to push into the
# `pdfs` bucket. Don't commit.
files/

# Per-part PNG thumbnails - rendered by tools/render_part_thumbnails.py and
# served from Supabase `thumbs` bucket in production. Don't commit.
public/models/thumbs/

# Local agent state - Claude Code session, MCP server config
.claude/
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Competition / business deliverables - keep out of the public repo
Anatom3D_Poslovni_Model.pdf
tehnicka_dokumentacija.pdf
TEHNICKA_DOKUMENTACIJA.md
Pravilnik Natjecanje LUMEN Development  2025._2026. (1).pdf
